{
  "name": "prims-browsers-cloud",
  "version": "0.1.0-alpha.1",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --experimental-strip-types --test test/*.test.ts",
    "typecheck": "tsc --noEmit",
    "verify": "npm run typecheck && npm test",
    "dry-run": "wrangler deploy --config apps/login/wrangler.jsonc --dry-run && wrangler deploy --config apps/gateway/wrangler.jsonc --dry-run"
  },
  "devDependencies": {
    "@types/node": "22.18.0",
    "typescript": "7.0.2",
    "wrangler": "4.129.0"
  },
  "engines": {
    "node": ">=22.18"
  },
  "dependencies": {
    "jose": "6.2.12"
  }
}
